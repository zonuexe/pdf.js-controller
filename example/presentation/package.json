{
  "private": true,
  "name": "presentation",
  "version": "1.0.0",
  "description": "",
  "main": "example.js",
  "scripts": {
    "start": "npm run build && static-server",
    "build": "esbuild example.js --bundle --format=esm --platform=browser --outfile=build.js && node copy-assets.js"
  },
  "author": "azu",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.11",
    "static-server": "^2.0.2"
  },
  "dependencies": {
    "pdf.js-controller": "../../"
  }
}
